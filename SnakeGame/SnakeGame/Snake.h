#pragma once
#include <vector>
#include <algorithm>

using namespace std;

class Snake
{
public:
	//Create the snake onto the board
	void CreateSnake();
	
	int x, y;
	unsigned int length;
private: 

protected:
	
};